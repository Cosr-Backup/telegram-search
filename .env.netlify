# Netlify environment variables for deployment
# These will be set in Netlify UI or through this file

# Telegram API credentials (optional, uses defaults if not set)
VITE_TELEGRAM_APP_ID="611335"
VITE_TELEGRAM_APP_HASH="d524b414d21f4d37f08684c1df41ac9c"

# Enable browser-only mode (use PGlite instead of backend server)
# Set to false to use PostgreSQL backend with Netlify Functions
VITE_WITH_CORE="true"

# Database configuration (for PostgreSQL backend)
# When using PostgreSQL, set DATABASE_TYPE to "postgres" and provide DATABASE_URL
# DATABASE_TYPE="postgres"
# DATABASE_URL="postgresql://user:password@host:port/database"

# Database debug mode (optional)
# DATABASE_DEBUG="false"

# Database debugger WebSocket URL (optional)
# VITE_DB_DEBUGGER_WS_URL=""

# Debug mode (optional)
# VITE_DEBUG="false"
